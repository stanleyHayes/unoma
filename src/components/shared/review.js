import {Card, CardActionArea, CardContent, CardHeader} from "@mui/material";

const Review = ({review}) => {
    return (
        <Card variant="outlined" sx={{borderRadius: 2, borderColor: "light.secondary"}}>
            <CardHeader />
            <CardContent>

            </CardContent>
            <CardActionArea>

            </CardActionArea>
        </Card>
    )
}

export default Review;
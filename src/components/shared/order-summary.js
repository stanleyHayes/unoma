import {Card, CardContent} from "@mui/material";

const OrderSummary = () => {

    return (
        <Card elevation={0} sx={{backgroundColor: ""}}>
            <CardContent>

            </CardContent>
        </Card>
    )
}

export default OrderSummary;